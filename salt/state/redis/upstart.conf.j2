{% set redis = pillar.get('redis', {}) -%}
start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 10 5

setuid redis
setgid redis

exec /usr/bin/redis-server /etc/redis/{{ redis.get('service', 'redis') }}.conf
